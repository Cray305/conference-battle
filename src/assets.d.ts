// The generated data file is imported with `with { type: "file" }`, which makes
// Bun copy it into the build and hand back its URL instead of its contents.
declare module "*/games.json" {
  const url: string;
  export default url;
}
